[project]
name = "pdoc-markdown"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "pdoc>=15.0.4",
]

[dependency-groups]
dev = [
    "bioshift",
]

[tool.uv.sources]
bioshift = { path = "../bioshift" }
